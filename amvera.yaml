meta:
  name: vb-skaner
  toolchain:
    name: node
    version: 20
build:
  command: npm install && npm run build
run:
  # Вместо npm run dev используем раздачу статики
  command: cp -r dist/* /usr/share/nginx/html/
