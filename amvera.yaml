meta:
  environment: node
  toolchain: npm
  version: 1.0
build:
  requirements: package.json
  script: |
    npm install
    npm run build
  artifacts:
    - dist
    - server.js
    - package.json
    - node_modules
run:
  script: npm start
  persistence: []
  ports:
    - 3000